---
task: 260414-1lv
type: quick
title: Update GSD plugin to 1.35.0 upstream version
scope: Sync upstream GSD v1.35.0 files into plugin layout, preserve local patches, bump versions
files_modified:
  - bin/lib/commands.cjs
  - bin/lib/config.cjs
  - bin/lib/core.cjs
  - bin/lib/docs.cjs
  - bin/lib/frontmatter.cjs
  - bin/lib/gsd2-import.cjs
  - bin/lib/init.cjs
  - bin/lib/intel.cjs
  - bin/lib/learnings.cjs
  - bin/lib/milestone.cjs
  - bin/lib/model-profiles.cjs
  - bin/lib/phase.cjs
  - bin/lib/profile-output.cjs
  - bin/lib/profile-pipeline.cjs
  - bin/lib/roadmap.cjs
  - bin/lib/schema-detect.cjs
  - bin/lib/security.cjs
  - bin/lib/state.cjs
  - bin/lib/template.cjs
  - bin/lib/uat.cjs
  - bin/lib/verify.cjs
  - bin/lib/workstream.cjs
  - bin/gsd-tools.cjs
  - references/*.md
  - contexts/dev.md
  - contexts/research.md
  - contexts/review.md
  - templates/phase-prompt.md
  - templates/AI-SPEC.md
  - package.json
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - README.md
autonomous: true
---

<objective>
Sync upstream GSD v1.35.0 into the plugin layout, preserving all local patches.

Purpose: Bring plugin up to date with two upstream releases (v1.34.0 through v1.35.0) which add the global learnings store, queryable codebase intelligence, 6+ new commands, 3 new runtimes (Cline, CodeBuddy, Qwen Code), statusline, worktree safety improvements, and 40+ bug fixes.

Output: All upstream-synced files updated, local patches re-applied, version bumped to 1.35.0 across all manifests and docs.
</objective>

<context>
@.planning/PROJECT.md
@package.json
@.claude-plugin/plugin.json
@.claude-plugin/marketplace.json
@README.md
@bin/lib/core.cjs (lines 1-61 — local resolveGsdRoot/resolveGsdDataDir/resolveGsdAsset)
@bin/gsd-tools.cjs (lines 953-1090 — local write-phase-memory, checkpoint, migrate, hooks handler)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone upstream v1.35.0 and sync files into plugin</name>
  <files>
    bin/lib/*.cjs (all upstream modules)
    bin/gsd-tools.cjs
    references/*.md (all)
    references/few-shot-examples/*.md (new)
    contexts/dev.md, contexts/research.md, contexts/review.md (new directory)
    templates/phase-prompt.md
    templates/AI-SPEC.md (new)
  </files>
  <action>
1. Clone upstream GSD at tag v1.35.0 to a temp directory:
   ```
   git clone --depth 1 --branch v1.35.0 https://github.com/gsd-build/get-shit-done.git /tmp/gsd-upstream-1.35.0
   ```

2. **Before copying anything**, capture the current local patches that must be preserved:

   a. **bin/lib/core.cjs local additions** (lines 1-61): Save the entire `resolveGsdRoot()`, `resolveGsdDataDir()`, and `resolveGsdAsset()` function block plus their exports. These three functions are appended after the upstream module.exports and also added to the exports object.

   b. **bin/gsd-tools.cjs local additions**: Save these custom sections:
      - The `write-phase-memory` command case (around line 953)
      - The `checkpoint` command case (around line 963)
      - The `migrate` command case (around line 1083)
      - The hooks handler block in the `hook` case (SessionStart auto-resume around line 989, PreCompact checkpoint around line 1046)
      - Any additional require statements for local modules (checkpoint.cjs, memory.cjs)

3. **Copy upstream bin/lib/*.cjs files**, EXCLUDING these plugin-local files that must NOT be overwritten:
   - `bin/lib/memory.cjs` (plugin addition)
   - `bin/lib/checkpoint.cjs` (v1.1 addition)

   New upstream modules to ADD (did not exist locally before):
   - `bin/lib/gsd2-import.cjs`
   - `bin/lib/intel.cjs`
   - `bin/lib/learnings.cjs`

4. **Copy upstream bin/gsd-tools.cjs** as the new base, then re-apply the local patches from step 2:
   - Insert `write-phase-memory` case into the command router switch
   - Insert `checkpoint` case into the command router switch
   - Insert `migrate` case into the command router switch
   - Insert the full hooks handler (SessionStart + PreCompact) into the `hook` case
   - Add the `require('./lib/checkpoint.cjs')` and `require('./lib/memory.cjs')` where needed (lazy requires inside case blocks are fine)
   - Add the `checkpoint` command to the help/usage comment block at the top

5. **Re-apply local patches to bin/lib/core.cjs**:
   - After copying the upstream core.cjs, add back the `resolveGsdRoot()`, `resolveGsdDataDir()`, and `resolveGsdAsset()` functions at the top of the file (after the initial require statements, before the first upstream function)
   - Add `resolveGsdRoot`, `resolveGsdDataDir`, `resolveGsdAsset` to the module.exports object at the bottom

6. **Copy all upstream references/**:
   - Overwrite existing `references/*.md` files with upstream versions
   - Add any new files (e.g., `references/ai-evals.md`, `references/ai-frameworks.md`, `references/common-bug-patterns.md`, `references/gates.md`, `references/ios-scaffold.md`, thinking-models-*.md, `references/thinking-partner.md`, `references/verification-overrides.md`)
   - Copy new `references/few-shot-examples/` subdirectory if present

7. **Copy new contexts/ directory** from upstream:
   - `contexts/dev.md`
   - `contexts/research.md`
   - `contexts/review.md`

8. **Copy upstream templates**:
   - Overwrite `templates/phase-prompt.md`
   - Add new `templates/AI-SPEC.md`
   - Do NOT overwrite other templates (they may have plugin-local modifications)

9. **Clean up** temp directory:
   ```
   rm -rf /tmp/gsd-upstream-1.35.0
   ```

CRITICAL: Do NOT touch these plugin-local directories/files during the sync:
- `mcp/` directory
- `skills/` directory
- `agents/` directory
- `hooks/` directory
- `.claude-plugin/` directory
- `migrations/` directory
- `bin/lib/memory.cjs`
- `bin/lib/checkpoint.cjs`
  </action>
  <verify>
    <automated>
node -e "
const core = require('./bin/lib/core.cjs');
const assert = (cond, msg) => { if (!cond) { console.error('FAIL: ' + msg); process.exit(1); } };

// Verify local patches survived
assert(typeof core.resolveGsdRoot === 'function', 'resolveGsdRoot missing from core.cjs exports');
assert(typeof core.resolveGsdDataDir === 'function', 'resolveGsdDataDir missing from core.cjs exports');
assert(typeof core.resolveGsdAsset === 'function', 'resolveGsdAsset missing from core.cjs exports');

// Verify local-only files untouched
const fs = require('fs');
assert(fs.existsSync('./bin/lib/memory.cjs'), 'memory.cjs was deleted');
assert(fs.existsSync('./bin/lib/checkpoint.cjs'), 'checkpoint.cjs was deleted');

// Verify new upstream modules exist
assert(fs.existsSync('./bin/lib/intel.cjs'), 'intel.cjs not copied from upstream');
assert(fs.existsSync('./bin/lib/learnings.cjs'), 'learnings.cjs not copied from upstream');

// Verify gsd-tools.cjs loads without errors
require('./bin/gsd-tools.cjs');

// Verify new directories/files exist
assert(fs.existsSync('./contexts/dev.md'), 'contexts/dev.md missing');
assert(fs.existsSync('./templates/AI-SPEC.md'), 'templates/AI-SPEC.md missing');

console.log('PASS: All sync verifications passed');
" 2>&1 || echo "Verification failed"
    </automated>
  </verify>
  <done>
    All upstream v1.35.0 files synced into plugin layout. Local patches (resolveGsdRoot/resolveGsdDataDir/resolveGsdAsset in core.cjs; write-phase-memory/checkpoint/migrate/hooks in gsd-tools.cjs) re-applied and functional. New upstream modules (intel.cjs, learnings.cjs, gsd2-import.cjs) present. New contexts/ directory and templates/AI-SPEC.md present. Plugin-local files (memory.cjs, checkpoint.cjs, mcp/, skills/, agents/, hooks/) untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump version to 1.35.0 across all manifests and docs</name>
  <files>
    package.json
    .claude-plugin/plugin.json
    .claude-plugin/marketplace.json
    README.md
  </files>
  <action>
Update version references from 1.33.0 to 1.35.0 in these files:

1. **package.json**: Change `"version": "1.33.0"` to `"version": "1.35.0"`

2. **.claude-plugin/plugin.json**: Change `"version": "1.33.0"` to `"version": "1.35.0"`

3. **.claude-plugin/marketplace.json**: Change `"version": "1.33.0"` to `"version": "1.35.0"` in the plugins array entry

4. **README.md**: Update the "Based on" line from:
   `**Based on:** [GSD 1.33.0](https://github.com/gsd-build/get-shit-done/releases/tag/v1.33.0)`
   to:
   `**Based on:** [GSD 1.35.0](https://github.com/gsd-build/get-shit-done/releases/tag/v1.35.0)`

5. **.planning/PROJECT.md**: Update the Context section reference from:
   `Based on [GSD 1.33.0]` to `Based on [GSD 1.35.0]`
   and update the release URL from `v1.33.0` to `v1.35.0`.
  </action>
  <verify>
    <automated>
node -e "
const fs = require('fs');
const assert = (cond, msg) => { if (!cond) { console.error('FAIL: ' + msg); process.exit(1); } };

const pkg = JSON.parse(fs.readFileSync('./package.json', 'utf8'));
assert(pkg.version === '1.35.0', 'package.json version not 1.35.0: ' + pkg.version);

const plugin = JSON.parse(fs.readFileSync('./.claude-plugin/plugin.json', 'utf8'));
assert(plugin.version === '1.35.0', 'plugin.json version not 1.35.0: ' + plugin.version);

const marketplace = JSON.parse(fs.readFileSync('./.claude-plugin/marketplace.json', 'utf8'));
assert(marketplace.plugins[0].version === '1.35.0', 'marketplace.json version not 1.35.0: ' + marketplace.plugins[0].version);

const readme = fs.readFileSync('./README.md', 'utf8');
assert(readme.includes('GSD 1.35.0'), 'README.md does not reference GSD 1.35.0');
assert(!readme.includes('GSD 1.33.0'), 'README.md still references GSD 1.33.0');

const project = fs.readFileSync('./.planning/PROJECT.md', 'utf8');
assert(project.includes('GSD 1.35.0'), 'PROJECT.md does not reference GSD 1.35.0');

console.log('PASS: All version bumps verified');
"
    </automated>
  </verify>
  <done>
    Version 1.35.0 reflected in package.json, plugin.json, marketplace.json, README.md, and PROJECT.md. No stale 1.33.0 references remain in version-tracked locations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Smoke-test plugin loads cleanly and commit</name>
  <files>None (verification only)</files>
  <action>
1. Run the gsd-tools.cjs entry point to verify it loads all modules without errors:
   ```
   node bin/gsd-tools.cjs --help 2>&1
   ```
   Should print usage without crash.

2. Verify the core module resolves paths correctly:
   ```
   node -e "const c = require('./bin/lib/core.cjs'); console.log('GSD root:', c.resolveGsdRoot()); console.log('Data dir:', c.resolveGsdDataDir());"
   ```

3. Verify new upstream modules load:
   ```
   node -e "require('./bin/lib/intel.cjs'); require('./bin/lib/learnings.cjs'); console.log('New modules load OK');"
   ```

4. If any module fails to load due to missing cross-references (e.g., intel.cjs importing from a module that changed shape), fix the import. Common issue: new modules may reference functions that moved or were renamed between v1.33.0 and v1.35.0.

5. Stage all changed files and create a single commit:
   ```
   git add bin/lib/*.cjs bin/gsd-tools.cjs references/ contexts/ templates/phase-prompt.md templates/AI-SPEC.md package.json .claude-plugin/plugin.json .claude-plugin/marketplace.json README.md .planning/PROJECT.md
   git commit -m "chore: sync upstream GSD v1.35.0 into plugin layout

Synced from get-shit-done v1.35.0 (tag 1274e0e).
New modules: intel.cjs, learnings.cjs, gsd2-import.cjs.
New directories: contexts/ (dev, research, review profiles).
New templates: AI-SPEC.md.
New references: ai-evals, ai-frameworks, common-bug-patterns, gates,
ios-scaffold, thinking-models, thinking-partner, verification-overrides,
few-shot-examples/.
Local patches preserved: resolveGsdRoot/resolveGsdDataDir/resolveGsdAsset
in core.cjs; write-phase-memory/checkpoint/migrate/hooks in gsd-tools.cjs.
Version bumped 1.33.0 -> 1.35.0 in package.json, plugin.json,
marketplace.json, README.md, PROJECT.md."
   ```
  </action>
  <verify>
    <automated>node bin/gsd-tools.cjs verify-path-exists .planning/PROJECT.md 2>&1 && echo "gsd-tools runs OK"</automated>
  </verify>
  <done>
    Plugin loads cleanly with all upstream v1.35.0 modules. All local patches functional. Single commit captures the full sync. Plugin is ready for use at version 1.35.0.
  </done>
</task>

</tasks>

<verification>
1. `node bin/gsd-tools.cjs --help` prints usage without errors
2. `node -e "const c = require('./bin/lib/core.cjs'); console.log(c.resolveGsdRoot())"` resolves correctly
3. `node -e "require('./bin/lib/intel.cjs'); require('./bin/lib/learnings.cjs'); console.log('OK')"` loads new modules
4. `git diff --stat HEAD` shows the expected set of changed/added files
5. No `1.33.0` version string remains in package.json, plugin.json, marketplace.json, README.md, PROJECT.md
</verification>

<success_criteria>
- All upstream v1.35.0 bin/lib/*.cjs, references/*.md, contexts/*.md, templates synced
- Local patches (core.cjs path resolution, gsd-tools.cjs custom commands and hooks) preserved and functional
- Plugin-local files (memory.cjs, checkpoint.cjs, mcp/, skills/, agents/, hooks/, migrations/) untouched
- Version 1.35.0 in all 5 manifest/doc files
- gsd-tools.cjs loads and runs without errors
- Single clean commit capturing the full sync
</success_criteria>
