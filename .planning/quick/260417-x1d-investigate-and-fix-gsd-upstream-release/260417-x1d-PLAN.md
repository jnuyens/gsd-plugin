---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "CronList shows an active gsd-upstream-check job running daily at 9:17am"
    - "The job uses the existing SKILL.md at ~/.claude/scheduled-tasks/gsd-upstream-check/SKILL.md"
  artifacts: []
  key_links:
    - from: "Claude Code cron system"
      to: "/Users/jnuyens/.claude/scheduled-tasks/gsd-upstream-check/SKILL.md"
      via: "CronCreate registration"
      pattern: "gsd-upstream-check"
---

<objective>
Register the GSD upstream release check as a daily cron job in Claude Code.

Purpose: The scheduled task SKILL.md was created in quick task 260407-4gi but CronCreate was never
called, so the job never ran. GSD 1.36.0 was released 3 days ago and went undetected. Registering
the cron job ensures daily checks going forward.

Output: A working daily cron job that checks for new GSD upstream releases at 9:17am every day.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jnuyens/.claude/scheduled-tasks/gsd-upstream-check/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SKILL.md and register cron job</name>
  <files></files>
  <action>
This task has two parts:

**Part A — Verify the SKILL.md is valid (executor does this):**
1. Read `/Users/jnuyens/.claude/scheduled-tasks/gsd-upstream-check/SKILL.md` and confirm it contains the version check logic (gh api call, version comparison, diff summary).
2. Run `cat /Users/jnuyens/claude-code-gsd/package.json | grep '"version"'` to confirm current plugin version is readable.
3. Run `gh api repos/gsd-build/get-shit-done/releases/latest --jq '.tag_name'` to confirm the API call works and returns a valid tag.

If both commands succeed, the SKILL.md is ready for registration.

**Part B — Register the cron job (ORCHESTRATOR must do this — executor cannot call CronCreate):**

The orchestrator MUST call CronCreate after executor completes Part A:
- Schedule: `17 9 * * *` (daily at 9:17am)
- Task description: Check for new upstream GSD releases and notify about available upgrades
- Instructions: Follow the steps in `/Users/jnuyens/.claude/scheduled-tasks/gsd-upstream-check/SKILL.md`

After registration, run CronList to confirm the job appears.
  </action>
  <verify>
    <automated>echo "Verify gh API works:" && gh api repos/gsd-build/get-shit-done/releases/latest --jq '.tag_name' && echo "OK"</automated>
  </verify>
  <done>CronList shows an active gsd-upstream-check job with daily schedule "17 9 * * *". The gh API call returns a valid upstream version tag.</done>
</task>

</tasks>

<verification>
- CronList returns at least one job matching "gsd-upstream-check" or "upstream GSD releases"
- The job schedule is daily (not weekly)
- The gh API endpoint is reachable and returns a version tag
</verification>

<success_criteria>
The GSD upstream release check cron job is registered and will run daily at 9:17am, ensuring
new releases like 1.36.0 are detected within 24 hours instead of being missed entirely.
</success_criteria>

<output>
After completion, create `.planning/quick/260417-x1d-investigate-and-fix-gsd-upstream-release/260417-x1d-SUMMARY.md`
</output>
